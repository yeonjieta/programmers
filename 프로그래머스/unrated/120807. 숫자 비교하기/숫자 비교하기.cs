using System;

public class Solution {
    public int solution(int num1, int num2) {
        int answer = -1;
        // 자바와 같은 연산자를 사용한다
        if(num1 == num2){
            answer = 1;
        }
        return answer;
    }
}